"use client";

import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { PageHeader, SectionCard } from "../../../components/AppShell";
import { useCharacters } from "../../../context/CharacterContext";
import { classes, races, backgroundNames } from "../../../lib/data";
import { getSubclassOptionsForClass } from "../../../lib/rules";
import type { AbilityScores } from "../../../lib/types";

const defaults: AbilityScores = { str: 10, dex: 10, con: 10, int: 10, wis: 10, cha: 10 };
const labels: Record<keyof AbilityScores, string> = { str: "Strength", dex: "Dexterity", con: "Constitution", int: "Intelligence", wis: "Wisdom", cha: "Charisma" };

export default function NewCharacterPage() {
  const router = useRouter();
  const { createCharacter } = useCharacters();
  const [form, setForm] = useState({ name: "", race: "Human", className: "Fighter", subclass: "", background: "Acolyte", alignment: "Unaligned", playerName: "", level: 1, hp: 10, maxHp: 10, ac: 10, speed: 30, hitDice: "1d10", proficiencyBonus: 2, abilities: defaults, notes: "" });
  const options = getSubclassOptionsForClass(form.className);
  function setField<K extends keyof typeof form>(key: K, value: (typeof form)[K]) { setForm((current) => ({ ...current, [key]: value })); }
  async function submit() { const id = await createCharacter(form); router.push(`/characters/${id}`); }
  return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><PageHeader eyebrow="Character Builder" title="Create Character" description="This creates a character in your Supabase account. The hierarchy controls what content appears as normally available." actions={<Link href="/characters" className="rounded-xl border border-stone-700 px-4 py-2.5 text-sm text-stone-300 hover:bg-stone-800">Cancel</Link>} /><div className="space-y-6"><SectionCard title="Identity"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Field label="Character name" value={form.name} required onChange={(value) => setField("name", value)} /><Select label="Race" value={form.race} options={races.map((r) => r.name)} onChange={(value) => setField("race", value)} /><Select label="Class" value={form.className} options={classes} onChange={(value) => { const next = getSubclassOptionsForClass(value); setForm((current) => ({ ...current, className: value, subclass: next[0]?.name ?? "" })); }} /><Select label="Subclass" value={form.subclass} options={options.map((o) => o.name)} onChange={(value) => setField("subclass", value)} disabled={options.length === 0} /><Select label="Background" value={form.background} options={backgroundNames} onChange={(value) => setField("background", value)} /><Field label="Player name" value={form.playerName} onChange={(value) => setField("playerName", value)} /><Number label="Level" value={form.level} min={1} max={20} onChange={(value) => setField("level", value)} /><Field label="Alignment" value={form.alignment} onChange={(value) => setField("alignment", value)} /></div></SectionCard><SectionCard title="Core stats"><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Number label="Current HP" value={form.hp} min={0} onChange={(value) => setField("hp", value)} /><Number label="Maximum HP" value={form.maxHp} min={1} onChange={(value) => setField("maxHp", value)} /><Number label="Armor Class" value={form.ac} min={0} onChange={(value) => setField("ac", value)} /><Number label="Speed" value={form.speed} min={0} onChange={(value) => setField("speed", value)} /><Field label="Hit Dice" value={form.hitDice} onChange={(value) => setField("hitDice", value)} /><Number label="Proficiency Bonus" value={form.proficiencyBonus} min={0} onChange={(value) => setField("proficiencyBonus", value)} /></div></SectionCard><SectionCard title="Ability scores"><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-6">{(Object.keys(defaults) as (keyof AbilityScores)[]).map((key) => <div key={key}><label className="text-xs font-semibold uppercase tracking-wider text-stone-500">{labels[key]}</label><input type="number" value={form.abilities[key]} onChange={(e) => setForm((c) => ({ ...c, abilities: { ...c.abilities, [key]: Number(e.target.value) } }))} className="mt-2 w-full rounded-xl border border-stone-700 bg-stone-950 px-3 py-2.5 text-sm text-stone-100 outline-none focus:border-amber-400" /></div>)}</div></SectionCard><SectionCard title="Notes"><textarea value={form.notes} onChange={(e) => setField("notes", e.target.value)} rows={8} className="w-full rounded-xl border border-stone-700 bg-stone-950 px-4 py-3 text-sm leading-6 text-stone-100 outline-none focus:border-amber-400" placeholder="Backstory, campaign notes, reminders..." /></SectionCard><div className="flex justify-end"><button type="button" onClick={() => void submit()} disabled={!form.name.trim()} className="rounded-xl bg-stone-100 px-5 py-3 text-sm font-semibold text-stone-950 hover:bg-amber-300">Create Character</button></div></div></div>;
}

function Field({ label, value, onChange, required = false }: { label: string; value: string; onChange: (value: string) => void; required?: boolean }) { return <label><span className="text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input required={required} value={value} onChange={(e) => onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-stone-700 bg-stone-950 px-3 py-2.5 text-sm text-stone-100 outline-none focus:border-amber-400" /></label>; }
function Number({ label, value, min, max, onChange }: { label: string; value: number; min?: number; max?: number; onChange: (value: number) => void }) { return <label><span className="text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><input type="number" min={min} max={max} value={value} onChange={(e) => onChange(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-stone-700 bg-stone-950 px-3 py-2.5 text-sm text-stone-100 outline-none focus:border-amber-400" /></label>; }
function Select({ label, value, options, onChange, disabled = false }: { label: string; value: string; options: string[]; onChange: (value: string) => void; disabled?: boolean }) { return <label><span className="text-xs font-semibold uppercase tracking-wider text-stone-500">{label}</span><select disabled={disabled} value={value} onChange={(e) => onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-stone-700 bg-stone-950 px-3 py-2.5 text-sm text-stone-100 outline-none focus:border-amber-400">{options.length === 0 ? <option value="">None</option> : options.map((option) => <option key={option}>{option}</option>)}</select></label>; }
