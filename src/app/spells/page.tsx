"use client";

import { useMemo, useState } from "react";
import { Badge, PageHeader, SectionCard } from "../../components/AppShell";
import { useCharacters } from "../../context/CharacterContext";
import { spells } from "../../lib/data";
import { getMaxSpellLevel, getSpellRestrictionReason, hasOverride, isSpellNormallyAvailable } from "../../lib/rules";

export default function SpellsLibraryPage() {
  const { characters, accessMode, addSpell } = useCharacters();
  const [characterId, setCharacterId] = useState(characters[0]?.id ?? "");
  const [search, setSearch] = useState("");
  const [level, setLevel] = useState("all");
  const [showRestricted, setShowRestricted] = useState(false);
  const character = characters.find((entry) => entry.id === characterId);
  const filtered = useMemo(() => spells.filter((spell) => `${spell.name} ${spell.school} ${spell.description}`.toLowerCase().includes(search.toLowerCase())).filter((spell) => level === "all" || String(spell.level) === level), [search, level]);

  return <div className="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8"><PageHeader eyebrow="Content Library" title="Spell Library" description="The catalogue contains content, while the selected character determines what is normally addable." /><SectionCard title="Filter access"><div className="grid gap-3 lg:grid-cols-[1fr_180px_220px]"><select value={characterId} onChange={(e) => setCharacterId(e.target.value)} className="rounded-xl border border-stone-800 bg-stone-950 px-4 py-3 text-sm"><option value="">No character selected</option>{characters.map((c) => <option key={c.id} value={c.id}>{c.name} • Level {c.level} {c.className}</option>)}</select><select value={level} onChange={(e) => setLevel(e.target.value)} className="rounded-xl border border-stone-800 bg-stone-950 px-4 py-3 text-sm"><option value="all">All levels</option>{Array.from({ length: 10 }, (_, i) => <option key={i} value={i}>{i === 0 ? "Cantrip" : `Level ${i}`}</option>)}</select><label className="flex items-center gap-2 text-sm text-stone-400"><input type="checkbox" checked={showRestricted} onChange={(e) => setShowRestricted(e.target.checked)} /> Show restricted content</label></div><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search spells..." className="mt-3 w-full rounded-xl border border-stone-800 bg-stone-950 px-4 py-3 text-sm outline-none focus:border-amber-400" /></SectionCard><div className="mt-6 space-y-3">{filtered.map((spell) => { const allowed = character ? isSpellNormallyAvailable(character, spell) || hasOverride(character, "spell", spell.id) : true; if (character && !showRestricted && !allowed) return null; const owned = character?.spells.some((entry) => entry.spellId === spell.id); return <article key={spell.id} className="rounded-2xl border border-stone-800 bg-stone-900/70 p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-semibold">{spell.name}</h2><Badge>{spell.level === 0 ? "Cantrip" : `Level ${spell.level}`}</Badge>{spell.classes.map((c) => <Badge key={c}>{c}</Badge>)}</div><p className="mt-3 text-sm leading-6 text-stone-400">{spell.description}</p><div className="mt-3 text-xs text-stone-600">{spell.school} • {spell.castingTime} • {spell.range} • {spell.duration}</div></div><div className="shrink-0">{character ? allowed ? owned ? <Badge tone="good">Already added</Badge> : <button onClick={() => addSpell(character.id, spell.id)} className="rounded-xl bg-stone-100 px-4 py-2.5 text-sm font-semibold text-stone-950">+ Add to {character.name}</button> : accessMode === "dm" ? <button onClick={() => addSpell(character.id, spell.id, false, true)} className="rounded-xl border border-amber-700 px-4 py-2.5 text-sm text-amber-300">DM Grant</button> : <div className="text-right"><Badge tone="warn">Restricted</Badge><p className="mt-1 max-w-xs text-xs text-stone-600">{getSpellRestrictionReason(character, spell)}</p></div> : <span className="text-xs text-stone-600">Select a character to check access</span>}</div></div></article>; })}</div></div>;
}
