name: Build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Test rules
        run: npm run test:rules
      - name: Export Supabase fuzz catalogue
        if: ${{ secrets.SUPABASE_SECRET_KEY != '' }}
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: npm run fuzz:catalogue

      - name: Fuzz rules
        run: npm run test:fuzz -- --iterations 5000 --seed 20260923
      - name: Build
        run: npm run build
